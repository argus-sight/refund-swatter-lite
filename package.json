{
  "name": "refund-swatter-lite",
  "version": "1.0.0",
  "description": "Simplified single-tenant Apple App Store refund prevention service",
  "scripts": {
    "deploy": "npm run deploy:db && npm run deploy:functions && npm run deploy:web",
    "deploy:db": "supabase db push",
    "deploy:functions": "supabase functions deploy",
    "deploy:web": "cd web && npm install && npm run build",
    "dev": "cd web && npm run dev",
    "setup": "./setup.sh"
  },
  "keywords": [
    "apple",
    "app-store",
    "refund",
    "consumption"
  ],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@supabase/supabase-js": "^2.56.1",
    "@types/jsonwebtoken": "^9.0.10",
    "jsonwebtoken": "^9.0.2",
    "tsx": "^4.20.5"
  }
}
